"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
import { useParams } from "next/navigation";
type Order={id:string;orderNumber:string;status:string;total:string|number;createdAt:string;shippingName:string;shippingPhone:string;addressLine1:string;city?:string;governorate?:string;discount:string|number;shippingFee:string|number;items:{productName:string;quantity:number;unitPrice:string|number}[]};
const labels:Record<string,string>={PENDING:"قيد المراجعة",CONFIRMED:"تم تأكيد الطلب",PROCESSING:"جاري التجهيز",SHIPPED:"تم الشحن",DELIVERED:"تم التوصيل",CANCELLED:"ملغي"};
const steps=["PENDING","CONFIRMED","PROCESSING","SHIPPED","DELIVERED"];
export default function OrderDetails(){const {id}=useParams<{id:string}>();const [o,setO]=useState<Order|null>(null);const [loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/orders/my").then(r=>r.json()).then((xs)=>setO(xs.find((x:Order)=>x.id===id)||null)).finally(()=>setLoading(false))},[id]);if(loading)return <main className="min-h-screen bg-zinc-950 p-8 text-white">جاري التحميل...</main>;if(!o)return <main className="min-h-screen bg-zinc-950 p-8 text-white"><Link href="/account">← رجوع</Link><div className="mt-8 rounded-2xl border border-zinc-800 bg-zinc-900 p-8 text-center">الطلب غير موجود.</div></main>;return <main className="min-h-screen bg-zinc-950 px-5 py-10 text-white"><div className="mx-auto max-w-4xl"><Link href="/account" className="text-zinc-400 hover:text-white">← رجوع للحساب</Link><div className="mt-6 rounded-2xl border border-zinc-800 bg-zinc-900 p-6"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm text-amber-400">ZENO STORE</p><h1 className="mt-1 text-3xl font-bold">طلب {o.orderNumber}</h1><p className="mt-1 text-sm text-zinc-500">{new Date(o.createdAt).toLocaleString("ar-EG")}</p></div><span className="h-fit rounded-full border border-amber-400/30 px-4 py-2 text-amber-300">{labels[o.status]||o.status}</span></div>{o.status!=="CANCELLED"&&<div className="mt-8 grid grid-cols-5 gap-2">{steps.map((s,i)=><div key={s}><div className={"h-2 rounded-full "+(steps.indexOf(o.status)>=i?"bg-amber-400":"bg-zinc-700")}/><p className="mt-2 text-center text-[10px] text-zinc-500">{labels[s]}</p></div>)}</div>}<section className="mt-8"><h2 className="mb-3 text-xl font-bold">المنتجات</h2><div className="space-y-3">{o.items.map((i,n)=><div key={n} className="flex justify-between gap-4 rounded-xl border border-zinc-800 p-4"><span>{i.productName} × {i.quantity}</span><span>{(Number(i.unitPrice)*i.quantity).toLocaleString("en-EG")} EGP</span></div>)}</div></section><section className="mt-8 grid gap-5 md:grid-cols-2"><div><h2 className="mb-3 text-xl font-bold">بيانات التوصيل</h2><div className="space-y-1 text-sm text-zinc-400"><p>{o.shippingName}</p><p>{o.shippingPhone}</p><p>{o.addressLine1}</p><p>{o.city} {o.governorate}</p></div></div><div><h2 className="mb-3 text-xl font-bold">ملخص الدفع</h2><div className="space-y-2 text-sm"><div className="flex justify-between"><span>الخصم</span><span>-{Number(o.discount).toLocaleString("en-EG")} EGP</span></div><div className="flex justify-between"><span>الشحن</span><span>{Number(o.shippingFee).toLocaleString("en-EG")} EGP</span></div><div className="mt-3 flex justify-between border-t border-zinc-800 pt-3 text-lg font-bold"><span>الإجمالي</span><span>{Number(o.total).toLocaleString("en-EG")} EGP</span></div></div></div></section></div></div></main>}